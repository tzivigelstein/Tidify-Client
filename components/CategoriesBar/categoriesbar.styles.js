import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
  },

  chip: {
    borderColor: 'hsl(11, 91%, 68%)',
    borderWidth: 1,
    margin: 8,
    height: 32,
    borderRadius: 999,
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'row',
    paddingHorizontal: 8,
  },

  chipText: {
    color: 'hsl(11, 91%, 58%)',
  },
})

export default styles
